{
  "name": "uncle-merns-book-search-engine",
  "version": "1.0.0",
  "description": "A GraphQL API built with Apollo Server, React front end and MongoDB database.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "eKirbs",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
